server:
  port: {{ server_port }}
spring:
  data:
    mongodb:
      host: {{ mongodb_host }}
      database: hello_ansible_web
zookeeper:
  connectString: {{ zookeeper_connectString }}
  leaderPath: "/hello-ansible-web/leadership"